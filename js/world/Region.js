/**
 * Represents a region in a World.
 *
 * Regions subdivide the world into ever-smaller pieces, until some atomic Block
 * is reached.
 */
export class Region {
    draw(context) { }
    addBlock(block) { }
}